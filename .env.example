# Supabase (Fase 1, passo 2)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# LLM — Groq (free tier). Pegue a key em https://console.groq.com/keys
GROQ_API_KEY=
# Opcional: força um modelo específico (ex.: llama-3.3-70b-versatile)
GROQ_MODEL=

# Dashboard — acesso único (MVP de demo, não é multi-usuário ainda)
# Gere um secret longo: openssl rand -hex 32
DASHBOARD_PASSWORD=
DASHBOARD_SESSION_SECRET=
